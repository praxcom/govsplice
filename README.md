# Govsplice
Public data for public benefit! A self hosted webapp to gain insights, not raw data, from gov.uk data.

## Documentation 
[Resources](docs/resources.md): Interesting & potentially useful resources influencing Govsplice


TO GET SECRET KEY:`openssl rand -hex 32`

`docker build -t "name:tag" <path or local>`
`docker images` to view
`docker run "name:tag"`

`docker run -p 80:80 "tag:name"` port forward
